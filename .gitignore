# ~/.config/nvim/.gitignore

# 插件管理器目录（如 lazy.nvim、packer 等）
/lazy-lock.json
#/packer_compiled.lua  # packer.nvim 的编译文件
#/plugin/packer_compiled.lua

# 运行时缓存和临时文件
# swap 文件忽视前三个 (一般不会超过3个)
*.swp
*.swo
*.swn
*.un~
*.log
*~

# 语言服务器和调试器缓存
#/lsp-logs/
#*.dSYM/  # macOS 调试符号

# 会话和备份文件
Session.vim
*.backup

# 本地测试文件
/test/
/tmp/

# 可选：个人本地覆盖配置（如机器特定设置）
/lua/local.lua
/lua/local.vim

